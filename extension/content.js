// Placeholder para leitura DOM ou comunicação com a página da corretora.
// Pode ser estendido para ler valores numéricos direto do DOM (mais confiável que OCR)
window.addEventListener('message', (ev)=> {
  // process messages if needed
});
